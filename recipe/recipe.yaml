schema_version: 1

context:
  version: "1.1.2"
  python_min: "3.10"

package:
  name: sfincs-jax
  version: ${{ version }}

source:
  url: https://pypi.org/packages/source/s/sfincs-jax/sfincs_jax-${{ version }}.tar.gz
  sha256: 4c4cef3e9f0632f17d36c02ce0c2467de99cd0bd72bce63949429ead1f02bb26

build:
  number: 0
  noarch: python
  script: ${{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  python:
    entry_points:
      - sfincs_jax = sfincs_jax.cli:main

requirements:
  host:
    - python ${{ python_min }}.*
    - setuptools
    - pip
  run:
    - python >=${{ python_min }}
    - jax
    - numpy
    - scipy
    - h5py
    - netcdf4
    - matplotlib-base

tests:
  - python:
      imports:
        - sfincs_jax
      pip_check: true
      python_version:
        - ${{ python_min }}.*
        - "*"
  - script:
      - sfincs_jax --help

about:
  summary: Neoclassical transport solver with CPU/GPU and differentiable JAX workflows
  license: MIT
  license_file: LICENSE
  homepage: https://sfincs-jax.readthedocs.io/
  repository: https://github.com/uwplasma/sfincs_jax
  documentation: https://sfincs-jax.readthedocs.io/

extra:
  recipe-maintainers:
    - rogeriojorge
    - matthewfeickert
